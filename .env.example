# Backend API base. Same-origin via Vercel rewrite in prod, direct in dev.
#
# Dev:  http://localhost:8000  (talks directly to local FastAPI)
# Prod: /api                   (Vercel rewrites /api/* → DuckDNS backend)
VITE_API_BASE=http://localhost:8000
